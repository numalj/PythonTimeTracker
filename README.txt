# Python Time Tracker

This is a python script that when run, tracks the time spent on each application. 

Google Chrome activity is monitored on a per url basis
The time statistics are stored in JSON format. 
